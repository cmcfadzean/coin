
<%= component "heading", title: "Budgets", stat: number_to_currency(@current_budget, :unit => "$") %>
<%= component "item", title: "", subtitle: "", link: "" do %>
  <%= link_to 'New Budget', new_budget_path %>
<% end %>
<% @budgets.each do |budget| %>
  <%= component "item", title: number_to_currency(budget.amount, :unit => "$"), subtitle: budget.year, link: edit_budget_path(budget) %>
<% end %>