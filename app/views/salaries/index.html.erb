<%= component "heading", title: @employee.name , stat: number_to_currency(@salaries.last.salary, :unit => "$") %>

<%= component "item", title: "", subtitle: "", link: "" do %>
  <%= link_to 'New Salary', new_employee_salary_path(params[:employee_id]) %>
<% end %>
<% @salaries.each do |salary| %>
  <%= component "item", title: salary.salary, subtitle: salary.effective.strftime("%B %d, %Y"), link: edit_employee_salary_path(salary.employee, salary) %>
<% end %>
